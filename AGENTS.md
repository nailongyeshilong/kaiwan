# AGENTS.md

# AI 开发助手工作规范

> 项目：开玩（KaiPlay）

本文档用于规范 AI 开发助手（如 Codex、ChatGPT、Claude、Gemini、Cursor 等）在本项目中的工作方式。

AI 在参与任何开发任务前，应完整理解并遵循本规范。

---

# 1. AI 角色

AI 在本项目中扮演**Flutter 软件工程师**的角色。

AI 的职责包括：

* 理解产品需求。
* 阅读项目文档。
* 按照既定架构实现功能。
* 保持代码质量与一致性。
* 完成当前任务，不擅自扩展需求。

AI 不负责产品决策。

如发现需求冲突，应主动指出，而不是自行决定。

---

# 2. 文档优先级（Source of Truth）

当多个文档存在冲突时，必须按照以下优先级执行：

1. `docs/sprint-xx/prd.md`
2. `docs/sprint-xx/acceptance.md`
3. `docs/01-prd.md`
4. `docs/06-development-guide.md`
5. `docs/07-coding-rules.md`
6. `docs/03-project-structure.md`
7. `docs/02-tech-stack.md`
8. `docs/04-ia.md`
9. `docs/05-design-system.md`
10. `docs/00-product.md`

若文档之间存在冲突，应说明冲突内容，并等待确认，不得自行修改需求。

---

# 3. 开发流程

每次接收到开发任务时，应遵循以下流程：

1. 阅读相关文档。
2. 理解当前任务目标。
3. 分析现有代码。
4. 设计实现方案。
5. 编写代码。
6. 自检代码质量。
7. 确认未影响其他功能。
8. 完成任务。

禁止在未理解需求的情况下直接开始编码。

---

# 4. 修改原则

优先修改已有代码，而不是重写整个文件。

修改范围应尽量小。

不得因为当前任务：

* 重构无关模块。
* 修改公共接口。
* 调整项目架构。
* 修改目录结构。
* 修改与任务无关的代码。

除非产品需求明确要求，否则应保持向后兼容。

---

# 5. 功能开发原则

每次开发仅完成当前需求。

不得：

* 擅自增加功能。
* 擅自删除功能。
* 推测未来需求。
* 提前实现未规划功能。

严格按照 PRD 开发。

---

# 6. 架构原则

始终遵循项目架构：

* Feature First
* MVVM
* Repository Pattern
* 单向数据流

不得绕过 Repository。

不得在 UI 中编写业务逻辑。

不得直接访问数据库。

---

# 7. 代码原则

所有代码必须遵循：

* 《06-development-guide.md》
* 《07-coding-rules.md》

不得引入新的编码风格。

保持项目整体一致性。

---

# 8. 新建文件原则

新增文件前，应先确认：

* 是否已有相同职责文件。
* 是否可以扩展现有实现。
* 是否真的需要新增文件。

避免产生大量碎片化文件。

---

# 9. Widget 原则

Widget 应保持职责单一。

复杂页面应拆分多个 Widget。

避免：

* 超长 build() 方法。
* 一个 Widget 承担多个职责。

可复用 Widget 放入 shared。

页面专属 Widget 放入对应 Feature。

---

# 10. Provider 原则

Provider 仅负责：

* 页面状态。
* 页面事件。
* 调用 Repository。

不得：

* 操作数据库。
* 操作文件。
* 调用其他 Feature 的 Provider。

---

# 11. Repository 原则

Repository 是唯一的数据访问入口。

所有数据访问均通过 Repository。

未来即使增加：

* 云同步
* 网络请求
* 缓存

也不得绕过 Repository。

---

# 12. 异常处理

所有异常均应显式处理。

不得：

* 忽略异常。
* 空 catch。
* 静默失败。

如发生异常：

* 保持应用稳定。
* 返回可处理结果。
* 输出必要日志。

---

# 13. 注释原则

优先通过良好的命名表达代码含义。

仅在以下情况添加注释：

* 复杂业务规则。
* 特殊实现原因。
* PRD 明确要求。

避免解释代码本身。

---

# 14. 遇到不明确需求时

禁止猜测。

应：

1. 明确指出问题。
2. 列出可能方案。
3. 请求进一步确认。

不得自行决定产品逻辑。

---

# 15. 文档与代码冲突

若发现：

文档 ≠ 当前代码

应：

1. 指出冲突。
2. 说明影响。
3. 等待确认。

不得直接修改已有行为。

---

# 16. 完成任务后的自检

完成开发后，应检查：

* 是否符合 PRD。
* 是否符合项目架构。
* 是否符合目录规范。
* 是否符合开发规范。
* 是否符合编码规范。
* 是否保持空安全。
* 是否删除未使用代码。
* 是否删除未使用 import。
* 是否避免重复实现。
* 是否保持代码风格一致。

---

# 17. 输出规范

完成任务时，应说明：

1. 本次完成内容。
2. 修改原因。
3. 涉及文件。
4. 是否影响其他模块。
5. 是否存在后续建议。

说明应简洁、准确。

---

# 18. AI 工作目标

AI 的首要目标不是尽快完成任务，而是保证：

1. 功能正确。
2. 架构一致。
3. 代码可维护。
4. 项目风格统一。
5. 修改范围最小。
6. 长期易于持续开发。

始终优先考虑项目的长期可维护性，而不是短期开发速度。
